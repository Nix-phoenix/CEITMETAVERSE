// ── Environment Config ────────────────────────────────────────────────────
// Single source of truth for the backend API base URL used by every page.
// Change the URL below (e.g. when the Cloudflare tunnel URL rotates) and
// every page picks it up automatically — no need to edit each HTML file.
window.__API_BASE__ = 'https://sic-beneath-ham-rep.trycloudflare.com';
